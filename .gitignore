# Este repositorio serve SO o frontend do painel (docs/).
# Nada de dado real e nada do backend entra aqui.

# o backend vive no Apps Script; copia no repo so envelhece e confunde
apps-script/
*.gs

# extratos, faturas, bases
*_Agosto.gs
*.csv
*.xlsx
*.ofx
*.pdf
dash_data.json
Lancamentos*.csv
